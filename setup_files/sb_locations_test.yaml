# ============================================================
# LaZagna Setup — SB Locations Test
# ============================================================
# Uses a custom CSV grid to control where 3D switch blocks
# are placed on the FPGA fabric.
# ============================================================

experiment_name: "sb_locations_test"

switch_block_3d:
  location_pattern: ["custom"]
  grid_csv_path: ["{lazagna_root}/setup_files/csv_patterns/rows_10x10_pattern.csv"]
